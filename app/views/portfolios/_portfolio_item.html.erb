<h3><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></h3><br>
    
    <%=portfolio_item.subtitle%><br>
    
    <%=portfolio_item.body%><br><br>
    
    <%= image_tag portfolio_item.thumb_image if !portfolio_item.thumb_image.nil? %>
    
    <%=link_to "Edit", edit_portfolio_path(portfolio_item) %>
    
    <%= link_to 'Destroy', portfolio_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?' } %>
<br>
<hr>
<br>